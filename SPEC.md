# SPEC — цикл B ServiceBooking: доводка недоделанного и фото к работе с клиентом

**Внимание к истории файла.** Предыдущая редакция этого документа описывала **цикл A (санация)** —
закрытие обходов бизнес-гейтов, межарендных утечек и корректности отчётов. Цикл A полностью завершён и
закоммичен; его SPEC живёт в git-истории, начиная с коммита `e6b746c` (`git show e6b746c:SPEC.md`),
более ранняя ревизия — `7c86ca2`. Настоящий документ его **заменяет**, а не продолжает.

**Отправная точка.** Ветка `sanitation-cycle`, коммит `75c5c3c`. Все факты ниже перепроверены чтением
исходников на этом коммите; ссылки «файл:строки» актуальны. `CURRENT_STATE.md` писался **до** цикла A:
его §4 местами описывает уже исправленное поведение (права на услуги, правило «24 часа», публичность
занятости, `companyId` в слотах, комиссия, тариф, дата в отчётах, Swagger, CI) — как источник фактов он
использовался только там, где утверждение подтверждено кодом. §5 (заглушки) и §9 (техдолг) актуальны.

**Тип работ.** Смешанный: (а) доводка того, что реализовано, но недостижимо через интерфейс;
(б) одна новая продуктовая функция — **фотофиксация работы мастера**, и, поверх её же безопасного
загрузчика, загрузка аватаров и картинок услуг. Сервис **не в продакшене**: ломающие миграции
допустимы, переносы данных не нужны.

**Чего в цикле нет.** Интеграция с мессенджером MAX исключена решением заказчика от 2026-09-02 и
вернётся отдельным анализом. Проведённое исследование и написанные под него истории сохранены целиком
в **приложении А** — повторять работу не нужно. Номера историй **US-01, US-02 и US-10…US-16 выведены из
обращения и не переиспользуются**: первые две отменены заказчиком (§0, Q8/Q9), остальные живут в
приложении А. Разрывы в нумерации оставлены намеренно, чтобы ссылки из приложения не поехали.

---

## 0. Решения заказчика

Все вопросы этого цикла закрыты 2026-09-02. Таблицы ниже — не «что спросить», а «что решено»:
именно по ним сверяются тела историй. Вопросы, которые упирались в способ отправки уведомлений
(прежние Q1–Q4, Q14, Q15), сняты вместе с интеграцией MAX и перенесены в приложение А — отвечать на
них сейчас не нужно.

### 0.1 Решения, определяющие объём работ

| # | Тема | Решение | Где отражено |
|---|---|---|---|
| **Q5** | Кто видит фото клиента | **Только сотрудники этой компании** — роли `Master` и `CompanyOwner`. Суперадмину содержимое недоступно, он видит лишь счётчики и занятый объём. Клиенту свои фото в этом цикле не показываются | US-18, US-19 |
| **Q6** | Согласие клиента на фотосъёмку | **В интерфейс ничего не добавляем.** Отдельного шага «клиент согласен» и текста-дисклеймера не будет. Аналитик отмечал юридический риск (фото человека — персональные данные, фото лица при ряде условий трактуется как биометрия); заказчик ознакомлен и принял решение осознанно, ответственность за законность обработки лежит на компании-салоне. Пункт стоит вернуть, если продукт пойдёт в продакшен | US-17 (шаг согласия убран), R6 |
| **Q7** | Сколько фото, как долго хранить, сколько места | До **5 фото на одну заметку**. **Срок хранения — опция тарифа**: 6 месяцев / 12 месяцев / бессрочно. **Квота на объём — тоже опция тарифа**; при превышении отказ с понятным текстом и предложением сменить тариф. **Сжатие на сервере применяется всегда**, независимо от тарифа: это техническая необходимость, а не настройка (без него оценка ~12 ГБ в сутки, §5.9 п. 5) | US-19, **US-24** (новая история: поля тарифа + админка), US-21 |
| **Q11** | `Service.ImageUrl` и `AppUser.AvatarUrl` нечем заполнить | **Реализовать загрузку в этом цикле** — поверх того же безопасного загрузчика, который пишется для фото к заметкам (проверка сигнатуры, сжатие, лимиты). Из DTO поля не убираем | **US-25** (новая история) |
| **Q16** | Кто может удалить заметку и её фото | **Автор или владелец компании.** Заметки общие для компании, а автор может уволиться — иначе его записи и фото станет некому убрать | US-07, US-20 |

### 0.2 Решения, закрывающие темы без работы

| # | Тема | Решение | Следствие |
|---|---|---|---|
| **Q8** | Текст на главной обещает «напоминание накануне визита» (`frontend/src/pages/HomePage.tsx:53`) | **Оставить как есть.** Рассчитываем, что уведомления появятся и текст станет правдой | Прежняя история **US-01 отменена** и из документа удалена |
| **Q9** | «Рассылка» рапортует об отправке, которой нет (`frontend/src/pages/owner/MailingTab.tsx:24`) | **Тему не трогаем в этом цикле.** Текст остаётся вводящим в заблуждение — это осознанное решение, а не упущение. Зафиксировано, чтобы не всплыло как «баг» на следующем ревью | Прежняя история **US-02 отменена** и из документа удалена. Эпик «интерфейс перестаёт обещать несуществующее» исчез целиком |
| **Q10** | Недостижимый статус `BookingStatus.Pending` | **Статус оставляем**, из enum и фильтров не убираем. Сценарий «запись ждёт подтверждения салона» проектируется отдельной историей позже | Из §7.3 «отложенное» убран как открытый вопрос; миграции не требуется |
| **Q12** | Мёртвые ветки в кабинете клиента: цена визита и «Записаться снова» | **Оживить.** Решение цикла A пересмотрено: добавляем `price` и `companySlug` в `BookingDto`, ветки UI восстанавливаем | US-22 (не удаление, а оживление) |
| **Q13** | «Мои визиты» скрыты от мастера и владельца | **Показывать всем аутентифицированным.** Мастер и владелец тоже бывают клиентами других салонов | US-08 |

**Что аналитик решил сам, вслед за исключением MAX** (не требует ответа, но должно быть видно):
часовой пояс компании и нормализация телефонов заводились ради напоминаний и **из цикла убраны** —
обоснование и последствия в §7.1.

---

## 1. Контекст: кто пользователь и какую проблему решает цикл

Продукт — SaaS онлайн-записи для салонов. После цикла A он безопасен и внутренне согласован. Цикл B
решает две разные задачи: доводит до пользователя то, что уже реализовано, но недостижимо, и добавляет
одну новую функцию — фото работы мастера.

| Роль | Проблема сегодня | Что меняет цикл |
|---|---|---|
| **Владелец компании** | Часть купленного не видна: виджет для сайта работает, но кода вставки нет; тариф, снятый с продажи, не вернуть; заблокированную компанию нечем разблокировать | Виджет выдаётся готовым кодом вставки; фотофиксация работы, чтобы мастер и следующий мастер видели, что делали в прошлый раз; объём фото и срок хранения становятся частью тарифа |
| **Мастер** | Заметку о клиенте можно написать, но нельзя удалить (сервер умеет, интерфейс не даёт). Фото работы приходится хранить в личном телефоне | Заметки становятся полноценными объектами: с автором, датой, удалением — и с прикреплёнными фото, видимыми в истории по клиенту |
| **Клиент** | При отмене визита никто не объясняет причину; в кабинете не видно ни цены, ни пути обратно в салон | Причина отмены видна обеим сторонам; в карточке визита есть цена и кнопка «Записаться снова» |
| **Суперадмин** | Блокировка компании и реактивация тарифа существуют в API и недостижимы в интерфейсе | Обе операции доступны из админки; в тарифе появляются лимиты на фото |
| **Команда** | Часть кода не вызывается ниоткуда и создаёт иллюзию функциональности при чтении. Единственный существующий загрузчик файлов небезопасен по содержимому | Мёртвый код убран; появляется один безопасный загрузчик изображений, на который переводятся все загрузки продукта |

**MVP цикла** = (1) всё, что уже реализовано и стоит копейки, чтобы стать достижимым; (2) фото к
заметке мастера с безопасной загрузкой и приватной раздачей, с лимитами из тарифа; (3) аватары и
картинки услуг поверх того же загрузчика.

**Сознательно откладывается:** любые уведомления (приложение А), email-рассылка, приём платежей,
пагинация, часовые пояса, нормализация телефонов, клиентский просмотр фото. Подробно — §7.3.

---

## 2. Приоритизация

Критерий — **сколько пользы приносит единица работы**. Ранжирование:

| Приоритет | Признак | Что попало |
|---|---|---|
| **P1 — новая ценность, ради которой цикл заказан** | Функциональность, которой в продукте не было | Фото (US-17…US-21, US-24), загрузка изображений (US-25) |
| **P2 — реализовано, но недостижимо** | Код есть, работает, стоит единиц строк довести | US-03…US-08 |
| **P3 — обеспечивающий техдолг** | Без него P1 нельзя сделать корректно | Заметка как объект (US-07), rate limiting на загрузку (US-19), фронтовый тест-раннер (US-23) |
| **P4 — гигиена** | Не видно снаружи | Удаление мёртвого кода (US-22) |

Прежний уровень «P0 — интерфейс обещает то, чего нет» исчез: обе его истории отменены заказчиком
(§0.2, Q8 и Q9).

---

## 3. Скоуп цикла

| Эпик | Содержание | Истории | Обязательность |
|---|---|---|---|
| **Э1** | Реализованное становится достижимым | US-03…US-08 | Обязательно |
| **Э2** | Фото к работе с клиентом | US-07 (предусловие), US-17…US-21, US-24 | Обязательно (Q5–Q7, Q16 решены) |
| **Э3** | Аватары, картинки услуг и логотип поверх безопасного загрузчика | US-25 | Обязательно (Q11) |
| **Э4** | Обеспечивающий техдолг | US-22, US-23 | Обязательно в объёме §7.1 |

**Уведомления через MAX (истории US-10…US-16) из цикла исключены** решением заказчика от 2026-09-02 —
тема возвращается к анализу отдельно, материал в приложении А. **Эпик «интерфейс перестаёт обещать
несуществующее» (US-01, US-02) отменён** решениями Q8 и Q9: содержания в нём не осталось.

**Если цикл не влезает** — режется в таком порядке (зафиксировано заранее, чтобы решение не
принималось в спешке):
1. **US-21** — автоочистка фото по сроку; заменяется задокументированной ручной процедурой, при этом
   поля тарифа из US-24 остаются и квота продолжает работать;
2. **US-25** — аватары и картинки услуг; загрузчик к этому моменту уже есть, история сводится к двум
   эндпоинтам и двум формам;
3. **US-05** — реактивация тарифа.

**Не режутся ни при каких условиях:** US-07 (без него нет эпика фото, §5.3) и US-19 (без него фото
нельзя выпускать вообще).

---

## 4. Э1 — реализованное становится достижимым

### 4.1 US-03 — код вставки виджета для сайта компании · P2

> **Как владелец компании, я хочу получить готовый код, чтобы вставить окно записи к себе на сайт.**

**Факт.** Роут `/embed/:slug` существует и работает (`frontend/src/App.tsx:34`), страница
`frontend/src/pages/EmbedPage.tsx` рендерит услуги компании и `BookingModal` без навбара. Ссылки на неё
нет нигде в приложении, генератора кода вставки нет.

**Критерии приёмки:**
1. В управлении компанией (`frontend/src/pages/owner/CompanyManagePage.tsx`, вкладка «Настройки»)
   появляется блок «Виджет для сайта»: прямая ссылка `https://<домен>/embed/<slug>`, готовый
   `<iframe …>`-сниппет и кнопка «Скопировать».
2. Сниппет содержит явные `width`/`height` (по умолчанию 100 % / 700 px), `title` для доступности и
   `loading="lazy"`.
3. Домен берётся из адреса текущей страницы, а не хардкодится.
4. Рядом — предупреждение, если у компании выключена самозапись (`allowSelfBooking = false`) или тариф
   не даёт онлайн-запись: в этом случае виджет покажет услуги, но запись из него не пройдёт.
   Используются уже отдаваемые флаги `CompanyDto` (`onlineBookingEnabled`).
5. Кнопка «Открыть предпросмотр» открывает `/embed/<slug>` в новой вкладке.
6. **Связь с безопасностью:** любые security-заголовки, запрещающие встраивание в iframe
   (`X-Frame-Options`/CSP `frame-ancestors`), должны разрешать `/embed/*` и запрещать остальное
   приложение. Это ограничение фиксируется в задаче для devops, даже если сами заголовки — не в этом
   цикле (`CURRENT_STATE.md` §9, US-26 старого бэклога).
7. Тестов бэкенда не порождает; ручная проверка: сниппет, вставленный в пустой HTML-файл, показывает
   рабочую форму записи.

### 4.2 US-04 — блокировка и разблокировка компании из админки · P2

> **Как суперадмин, я хочу заблокировать компанию-нарушителя и снять блокировку, не залезая в базу.**

**Факт.** `PUT /api/admin/companies/{id}` принимает `{ name, isActive, allowSelfBooking }` и работает.
В api-слое фронта метод есть — `frontend/src/api/admin.ts:93-94` (`updateCompany`), — но **не вызывается
ниоткуда**. В интерфейсе видно только следствие: бейдж «Заблокирована» (`AdminPage.tsx:224`), а
единственная кнопка в карточке — «Сменить владельца» (`:230`). Блокировка при этом содержательна:
`Company.IsActive = false` убирает компанию из публичного каталога (`CompaniesController.cs:24`),
со страницы по слагу (`:67`), из списков владельца и участника (`:42`, `:57`) и из публичного списка
мастеров (`:83`).

**Критерии приёмки:**
1. В карточке компании во вкладке «Компании» — кнопка «Заблокировать» / «Разблокировать» в зависимости
   от `isActive`.
2. Блокировка требует подтверждения в модальном окне с перечислением последствий: компания исчезает из
   каталога, её страница перестаёт открываться, сотрудники не видят её в кабинете. Существующие записи
   **не отменяются** — это фиксируется в тексте.
3. Ошибка показывается текстом рядом с кнопкой через существующий маппер (`src/utils/planError.ts`
   или новый по конвенции `src/utils/*Error.ts`).
4. После успеха инвалидируются ключи `['admin-companies']`.
5. Тест: `ADM-0xx` блокировка → компания пропадает из `GET /api/companies` и `GET /api/companies/{slug}`
   даёт 404; разблокировка возвращает её.
6. **Предположение: блокировка не отменяет уже созданные записи и не отзывает роли**
   *(низкий риск — поведение сервера уже такое, история его не меняет).*

### 4.3 US-05 — деактивированный тариф можно вернуть в продажу · P2 *(режется третьим, §3)*

> **Как суперадмин, я хочу снова включить тариф, который убрал по ошибке, не создавая его заново.**

**Факт.** `frontend/src/pages/admin/PlansTab.tsx:218-232`: неактивные планы выводятся списком без единой
кнопки. На бэкенде препятствия нет — `PUT /api/admin/plans/{id}` принимает `isActive`, а
`formToPayload()` (`:64-77`) вообще всегда шлёт `isActive: true`; не хватает только точки входа в UI.

**Критерии приёмки:**
1. У каждого неактивного плана — кнопка «Активировать», отправляющая `PUT` с текущими значениями плана
   и `isActive: true`.
2. У неактивного плана также доступно «Редактировать» — открывается та же форма; сохранение по-прежнему
   возвращает план в активные (поведение `formToPayload` не меняется, но становится осознанным и
   комментируется в коде).
3. Ошибка сохранения показывается через `getPlanErrorMessage`, как у соседних мутаций.
4. Тест: `ADM-0xx` деактивация → активация → план снова виден как активный и назначается владельцу
   без ошибки (регрессия к правилу US-08 цикла A: назначить можно только активный план).

### 4.4 US-06 — причина отмены доходит до сервера и видна людям · P2

> **Как клиент, я хочу видеть, почему салон отменил мой визит; как владелец — понимать, почему клиент
> отменился.**

**Факт.** `BookingsController.cs:456-471` принимает `[FromBody] string? reason` и пишет
`booking.CancellationReason`. Api-слой фронта это поддерживает (`frontend/src/api/bookings.ts:47-48`),
но **ни один экран причину не передаёт**: `ClientBookingsPage.tsx:53` и `MyBookingsPage.tsx:245`
вызывают `bookingsApi.cancel(id)` без второго аргумента. Поле всегда пустое. Обратно причина не
отдаётся вовсе — `MapToDto` (`BookingsController.cs:502-506`) её не включает.

**Критерии приёмки:**
1. Отмена в кабинете мастера (`MyBookingsPage`) открывает маленькую форму с необязательным полем
   «Причина (увидит клиент)», ограниченным 300 символами, и кнопками «Отменить запись»/«Назад».
2. Отмена в кабинете клиента (`ClientBookingsPage`) — то же с текстом «Причина (увидит салон)».
3. `BookingDto` получает поле `cancellationReason`; `MapToDto` его заполняет; TS-тип `Booking`
   обновляется вручную по конвенции (`frontend/src/types/index.ts`). Этим же изменением добавляются
   `price` и `companySlug` (решение Q12, см. US-22).
4. У отменённой записи причина показана в обоих кабинетах; при пустой причине блок не рисуется.
5. Серверная валидация: `[MaxLength(300)]` на причину, `400` при превышении.
6. Тесты: `BK-0xx` отмена с причиной → `GET /api/bookings/{id}` возвращает её; `BK-0xy` причина длиннее
   300 символов → `400`; `BK-0xz` отмена без причины → `204`, поле `null` (регрессия существующего
   поведения).

### 4.5 US-07 — заметка о клиенте становится объектом: id, автор, дата, удаление · P2/P3, **предусловие эпика фото**

> **Как мастер, я хочу удалить свою заметку о клиенте — например, ошибочную, — и видеть, кто и когда
> написал остальные.**

**Факт.** `DELETE /api/masters/clients/notes/{id}` реализован и корректно ограничен автором
(`MastersController.cs:127-139`), в api-слое фронта метод есть (`frontend/src/api/masters.ts:20-21`), но
**вызвать его нечем**: заметки отдаются **массивом голых строк** — `.Select(n => n.Note)` в
`MastersController.cs:51` и `:78`, тип `notes: string[]` в `masters.ts:11`. Идентификатора у заметки в
ответе нет, автора и даты — тоже, хотя `ClientNote` хранит и `MasterId`, и `CreatedAt`
(`ServiceBooking.Core/Entities/ClientNote.cs:10,14`). Поэтому кнопку удаления в интерфейсе не к чему
привязать, и её там нет.

**Почему это часть эпика фото, а не побочная находка.** Фото прикрепляются к заметке (§5.2). Пока
заметка в ответе API — строка, к ней нельзя привязать ни фото, ни удаление, ни автора. Переделка формы
выдачи заметок — **первая задача эпика Э2**, а не сопутствующее улучшение; US-17…US-21 без неё не
кодируются.

**Критерии приёмки:**
1. `GET /api/masters/clients` отдаёт заметку объектом: `id`, `note`, `createdAt`, `authorId`,
   `authorName`, `bookingId` (см. §5.2), `photos` (см. US-18), `canDelete`. Это **ломающее изменение
   контракта**, фиксируется в `API_CONTRACT.md` и в TS-типе `MasterClient`.
2. Право удаления — автор заметки **или** владелец компании (решение Q16); серверная проверка
   расширяется в `MastersController.DeleteNote` (`:132`), где сейчас условие `n.MasterId == userId`.
3. В обоих местах, где показываются заметки — `MasterClientsPage.tsx:104-117` и
   `MyBookingsPage.tsx:86-98` — у заметки видны автор и дата, а при `canDelete` — кнопка удаления с
   подтверждением.
4. После удаления инвалидируется `['master-clients', companyId]`.
5. Терминология приводится в порядок в этой же истории (§5.1): кнопка «Отзыв о клиенте»
   (`MyBookingsPage.tsx:203`) и плейсхолдер «Оставить отзыв о клиенте…» (`:104`) переименовываются в
   «Заметки о клиенте» / «Добавить заметку…». Слово «отзыв» в интерфейсе остаётся только за `Review`.
6. Тесты: `MC-0xx` автор удаляет свою заметку → `204`, её нет в выдаче; `MC-0xy` владелец компании
   удаляет чужую заметку → `204`; `MC-0xz` мастер удаляет заметку коллеги → `403`/`404` (какой именно —
   фиксирует `API_CONTRACT.md`); `MC-0xw` выдача содержит `id`, `createdAt` и автора.

### 4.6 US-08 — «Мои визиты» доступны мастеру и владельцу из меню · P2

> **Как мастер, который сам записывается в другой салон, я хочу попадать в свои визиты из меню.**

**Факт.** Роут `/my-visits` разрешён ролям `Client, Master, CompanyOwner, SuperAdmin`
(`frontend/src/App.tsx:54-58`), но пункт меню показывается только при `isClient`, а `isClient` по
построению исключает мастера, владельца и админа (`Navbar.tsx:19-21,40-42,95-97`). Страница достижима
только по прямому URL.

**Критерии приёмки:**
1. Пункт «Мои визиты» виден любому аутентифицированному пользователю — и в десктопном, и в мобильном
   меню (решение Q13).
2. Порядок пунктов для персонала: «Мои записи» (рабочий календарь) → «Мои визиты» (личные) → «Кабинет»
   → «Админ». Названия не меняются, чтобы не ломать привычку.
3. Ручная проверка: под владельцем оба раздела открываются и показывают разные наборы данных.

---

## 5. Э2 — фото к работе с клиентом

### 5.1 Термины: речь о заметках мастера, а не об отзывах клиента

Заказчик подтвердил: имеются в виду **заметки мастера о клиенте** (`ClientNote`), слово «отзывы» в
исходной формулировке было оговоркой. **Отзывы клиента о компании (`Review`) эта задача не затрагивает
вообще** — ни загрузки, ни просмотра фото для них не проектируем.

Развилки нет, но различие сущностей нужно держать в голове при работе — в интерфейсе они сейчас
называются одинаково:

| | `Review` — **не трогаем** | `ClientNote` — **сюда крепятся фото** |
|---|---|---|
| Кто пишет | Клиент | Мастер |
| О ком | О компании и мастере | О клиенте |
| Где видно | Публично на странице компании (`CompanyPage.tsx`) | Только персоналу компании: карточка клиента (`MasterClientsPage.tsx:104-117`) и раскрывающаяся панель под записью (`MyBookingsPage.tsx:86-98`) |
| Ограничения | Один на запись (уникальный индекс), только по завершённой записи, только от владельца записи (`ReviewsController`) | Общие для компании, автор — мастер, удаляет автор (`MastersController.cs:127-139`) |

Путаница закреплена в коде и её нужно устранить (US-07 п. 5): кнопка, раскрывающая **заметки**,
подписана «Отзыв о клиенте» (`MyBookingsPage.tsx:203`), а поле ввода заметки — «Оставить отзыв о
клиенте…» (`:104`). Рядом стоит кнопка «Отзыв об услуге» (`:196-199`), которая **никогда не
отрисовывается** (см. US-22): она показывается по списку `can-review`, а тот отдаёт только записи, где
мастер сам был клиентом.

### 5.2 К чему привязываем фото — решение и обоснование

**Решение: фото прикрепляются к заметке (`ClientNote`); сама заметка получает необязательную ссылку на
визит (`BookingId`).**

Почему так, а не иначе:
- **Не к `Booking` напрямую.** Тогда фото не попадут в «историю по клиенту» без второго механизма
  выборки: заметки уже собраны по клиенту и по компании в `MastersController.GetClients` и показываются
  ровно в тех двух местах, куда просятся фото. Кроме того, к гостю без аккаунта заметки привязываются
  по телефону — механизм уже отлажен и фото получают его бесплатно, а привязка к записи потребовала бы
  его продублировать.
- **Не отдельной сущностью «фотоальбом клиента».** Она дублирует то, чем уже является заметка (запись
  сотрудника о клиенте), и удваивает набор прав доступа — а права здесь самое дорогое.
- **Ссылка на визит всё же нужна:** без неё в истории видно «фото», а не «фото с визита 12 марта,
  стрижка». Поле необязательное — заметку можно оставить и вне визита, как сейчас.
- Практическое подтверждение: панель заметок уже открывается **прямо под записью** в кабинете мастера
  (`MyBookingsPage.tsx:201-205, 222`), то есть ровно в момент «мастер поработал с клиентом». Фото
  добавляются в существующий сценарий, а не создают новый.

**Схема (эскиз для архитектора, форму фиксирует он):** `ClientNote` получает `BookingId?`; новая
сущность фото хранит ссылку на заметку, относительный путь к файлу, тип содержимого, размер, размеры
изображения, кто и когда загрузил. Каскад: удаление заметки удаляет фото и файлы (US-20).

### 5.3 Предусловие: заметка должна стать объектом

Эпик Э2 **начинается с US-07** (§4.5): сегодня заметки отдаются массивом голых строк
(`MastersController.cs:51,78`; `masters.ts:11`), и прикрепить к ним фото, автора или удаление физически
не к чему. Это часть работы эпика, а не отдельное улучшение; истории US-17…US-21 не кодируются, пока
US-07 не смёржена.

### 5.4 US-17 — мастер прикрепляет фото к заметке о клиенте · P1

> **Как мастер, я хочу приложить к заметке несколько фото сделанной работы, чтобы в следующий раз я
> или коллега видели, что делали этому клиенту.**

**Критерии приёмки:**
1. В панели под записью (`MyBookingsPage`, `ClientHistoryPanel`) и в карточке клиента
   (`MasterClientsPage`) при создании заметки можно приложить **до 5 фото** (решение Q7). Загрузка —
   с камеры или из галереи (`accept="image/*"`, `capture` на мобильных).
2. Фото можно добавить к уже существующей заметке и удалить по одному (право — по Q16, см. US-20).
3. **Шага «клиент дал согласие» в интерфейсе нет** — решение Q6. Никаких чекбоксов, дисклеймеров и
   хранения факта согласия не добавляется; ответственность за законность съёмки и хранения лежит на
   компании-салоне. Это осознанное решение заказчика, а не упущение: при выходе в продакшен к нему
   стоит вернуться (R6).
4. Заметка, созданная из панели под записью, автоматически получает ссылку на эту запись (`BookingId`).
5. Загрузка показывает прогресс и понятную ошибку (маппер по конвенции `src/utils/*Error.ts`):
   слишком большой файл, неподдерживаемый формат, превышен лимит фото на заметку, исчерпана квота
   компании (с указанием тарифа, где она больше).
6. Тесты: `MC-1xx` мастер компании загружает фото → `201` и фото в выдаче; `MC-1xy` посторонний → `403`;
   `MC-1xz` шестое фото к заметке → `400`.

### 5.5 US-18 — фото видны в истории по клиенту · P1

**Критерии приёмки:**
1. `GET /api/masters/clients` отдаёт у каждой заметки список фото: `id`, дата, автор, размер,
   `thumbnailUrl`/`url` — **ссылки на защищённый эндпоинт**, а не на статику (см. US-19 п. 4).
2. В обоих экранах фото показываются миниатюрами в ленте заметок, по клику — просмотр в модальном окне
   с навигацией между фото одной заметки.
3. Миниатюры лениво загружаются (`loading="lazy"`) — в карточке клиента с длинной историей их может
   быть много.
4. У фото видно, к какому визиту оно относится (дата и услуга), если ссылка на визит есть.
5. Доступность: у изображений осмысленный `alt` («Фото работы, 12 марта, стрижка»), модалка
   закрывается по Esc через существующий `useOverlayDismiss`.
6. Круг доступа — по решению Q5: только сотрудники этой компании (`Master`/`CompanyOwner`); суперадмин
   содержимого не получает; клиенту свои фото в этом цикле не показываются.

### 5.6 US-19 — безопасная загрузка и приватная раздача · P1, **не режется**

> **Как владелец компании, я хочу быть уверен, что фото моих клиентов не увидит посторонний и что
> через загрузку нельзя навредить серверу.**

**Факт-предупреждение.** Единственная существующая загрузка — логотип компании
(`CompaniesController.cs:262-303`): проверяется только `Content-Type` и размер, файл кладётся в
`wwwroot/uploads/companies` и раздаётся `UseStaticFiles` **любому, кто знает ссылку**; лимита на число
загрузок нет. Для логотипа это приемлемо (он публичен), для фото клиента — **нет**. Ревью прошлого цикла
отметило оба пробела; новая функция их повторять не должна.

**Критерии приёмки:**
1. **Проверка содержимого по сигнатуре файла** (magic bytes) для JPEG, PNG, WEBP. `Content-Type` и имя
   файла клиента не являются источником истины ни для решения о приёме, ни для расширения на диске.
2. Лимит **5 МБ на файл** (согласуется с `client_max_body_size 6M` в `deploy/nginx/ezbook.conf` —
   при изменении лимита nginx правится тем же PR, задача для devops).
3. **Изображение пересохраняется сервером всегда, независимо от тарифа** (решение Q7): ресайз до
   ~1600 px по длинной стороне, перекодирование в JPEG/WEBP, **удаление EXIF** — в том числе геометки и
   модели устройства. Исходный файл не сохраняется. Пересохранение заодно обезвреживает файлы-полиглоты.
4. **Файлы лежат вне `wwwroot`** (отдельный каталог из конфигурации, отдельный docker volume) и
   **не раздаются `UseStaticFiles`**. Раздача — через эндпоинт `GET /api/client-notes/photos/{id}`,
   который проверяет, что вызывающий — сотрудник (`Master`/`CompanyOwner`) той же компании, и только
   тогда отдаёт поток. Имена файлов — `Guid`, но безопасность на них не строится.
   **Это первый в продукте приватный класс хранения**; публичные изображения (логотип, аватар, картинка
   услуги) остаются в `wwwroot` — граница между двумя классами описывается явно (§11 п. 2).
5. Ограничение частоты: не более N загрузок в минуту на пользователя (значение фиксирует архитектор),
   `429` при превышении. Это первое место в продукте с rate limiting — механизм вводится точечно, не
   переписывая аутентификацию.
6. **Квота компании берётся из тарифа** (US-24): при исчерпании — `400` с текстом, сколько занято,
   сколько доступно и на каком тарифе больше. Занятое место считается по сумме размеров в БД, а не
   обходом диска.
7. **Суперадмин содержимого фото не получает** (Q5): ему доступны только счётчики и объём.
8. Тесты: `MC-2xx` файл с расширением `.jpg`, но содержимым не-изображения → `400`; `MC-2xy` доступ к
   фото чужой компании → `403`; `MC-2xz` аноним → `401`; `MC-2xw` файл 6 МБ → `400`/`413`;
   `MC-2xv` EXIF отсутствует в сохранённом файле (проверяется чтением сохранённого байтового потока);
   `MC-2xu` загрузка при исчерпанной квоте → `400`, файл на диск не записан.

### 5.7 US-20 — удаление, каскады и жизненный цикл файлов · P1

**Критерии приёмки:**
1. Удаление фото: доступно автору заметки и владельцу компании (Q16); удаляется и строка, и файл с диска.
2. Удаление заметки (US-07) удаляет все её фото и файлы. Каскад описан в `AppDbContext` явно, а не
   опирается на поведение по умолчанию.
3. Удаление компании / удаление аккаунта клиента: файлы удаляются вместе с данными. **Сервис не в
   продакшене — ломающие миграции допустимы**, миграция переноса не требуется.
4. **Удаление мастера из компании (`RemoveMember`) фото не удаляет**: заметки и фото — данные компании,
   а не сотрудника (это уже зафиксировано в комментарии к `ClientNote`). Право удаления по осиротевшим
   заметкам остаётся у владельца компании. Решение комментируется в коде.
5. Удаление записи (`Booking`) в продукте отсутствует — записи только отменяются; отмена визита фото
   **не** удаляет (работа могла быть выполнена частично). Если `DELETE` появится позже — `BookingId`
   у заметки обнуляется, заметка и фото остаются.
6. При любом удалении строка в БД и файл на диске в худшем случае расходятся в сторону «файл остался
   сиротой» — сирот подчищает уборка из US-21. Обратный порядок (файл удалён, строка осталась)
   недопустим: сначала БД, потом диск.
7. Освобождённый объём немедленно возвращается в квоту компании (US-24) — счётчик пересчитывается в той
   же транзакции, а не раз в сутки.
8. Тесты: `MC-3xx` удаление заметки удаляет фото (проверка отсутствия файла); `MC-3xy` чужой мастер
   удаляет фото → `403`; `MC-3xz` владелец удаляет фото своего мастера → `204`;
   `MC-3xw` после удаления занятый объём уменьшился.

### 5.8 US-24 — срок хранения и объём фото как опции тарифа · P1, **новая история по решению Q7**

> **Как суперадмин, я хочу задавать в тарифе, сколько места под фото и как долго они хранятся, чтобы
> продавать это как часть тарифа, а не раздавать всем поровну.**

**Факт.** Сегодня `SubscriptionPlanConfig` описывает только функциональные флаги и лимиты сотрудников
и компаний; поля про хранение файлов в нём нет. Разрешение тарифа идёт через `SubscriptionResolver`
с падением в `EffectivePlan.Free` при отсутствии/просрочке/неактивности подписки — новые поля обязаны
пройти тот же путь, иначе владелец без подписки получит `0` или `null` и загрузка сломается.

**Критерии приёмки:**
1. `SubscriptionPlanConfig` получает два поля: **квота на объём фото** (в мегабайтах; отдельное
   значение «без ограничения») и **срок хранения** — перечисление из трёх значений: 6 месяцев,
   12 месяцев, бессрочно (ровно те, что назвал заказчик; произвольное число дней не вводим).
2. Оба поля попадают в `EffectivePlan` и разрешаются `SubscriptionResolver` по тем же правилам, что
   остальные возможности, **включая Free-базлайн**.
   *Предположение: Free = 100 МБ и 6 месяцев (низкий риск — одно значение в одном месте, меняется до
   мёржа).*
3. В админке (`pages/admin/PlansTab.tsx`) оба поля редактируются в форме тарифа и показываются в
   карточке плана рядом с существующими лимитами; значения валидируются (`400` на отрицательную квоту).
4. Владелец видит свои лимиты в настройках компании: «занято X из Y МБ», «фото хранятся N месяцев».
   При приближении к квоте (>90 %) — предупреждение с предложением сменить тариф.
5. Переход на тариф с меньшей квотой **не удаляет уже загруженные фото**: новые загрузки блокируются
   до освобождения места. *Предположение (низкий риск), но фиксируется явно — это ровно тот случай,
   где молчаливое удаление данных было бы катастрофой.*
6. Сокращение срока хранения действует на уже загруженные фото при следующей уборке (US-21) — это
   поведение описывается в тексте админки, чтобы суперадмин понимал последствие.
7. Тесты: `ADM-0xx` создание/редактирование плана с новыми полями; `ADM-0xy` отрицательная квота →
   `400`; `MC-2xu` (из US-19) — отказ загрузки при исчерпанной квоте; юнит-кейсы `SubscriptionResolver`
   на Free-базлайн и на неактивный план.

### 5.9 US-21 — автоочистка по сроку хранения и место на диске · P1 *(режется первым, §3)*

**Критерии приёмки:**
1. Фоновая уборка: раз в сутки удаляет фото старше срока хранения тарифа (US-24) и файлы-сироты.
   **Это первый и единственный фоновый процесс в продукте** — до сих пор в решении не было ни
   `BackgroundService`, ни `IHostedService` (`CURRENT_STATE.md` §5.1 п. 3). Требования к нему: не
   более одного исполнителя одновременно (advisory lock по конвенции проекта), устойчивость к
   перезапуску, работа порциями, чтобы не держать транзакцию на тысячах строк.
2. Перед удалением по сроку в журнал пишется, сколько удалено и сколько освобождено; занятый объём
   компаний пересчитывается.
3. При нехватке места на диске (порог настраивается) загрузка отклоняется с понятным текстом, а не
   падает с необработанной ошибкой записи файла.
4. Тариф «бессрочно» означает, что уборка такие фото не трогает вовсе — только сирот.
5. **Если история режется** (§3): срок хранения из тарифа продолжает отображаться, но не применяется
   автоматически; вместо этого в `DEPLOY.md` описывается ручная процедура (SQL-выборка + скрипт
   удаления файлов), и это **явно фиксируется в `CHANGELOG` как незавершённая часть**, чтобы владельцы
   не считали, что фото удаляются сами.
6. **Оценка ёмкости, ради которой всё это существует:** без сжатия 5 фото по 4 МБ на визит × 20 визитов
   в день × 50 компаний ≈ **20 ГБ в сутки** — типовой VPS кончится за пару дней. После ресайза из
   US-19 п. 3 (≈300 КБ на фото) те же цифры дают ≈1,5 ГБ в сутки, что всё равно требует квоты и срока
   хранения. **Ресайз, квота и уборка — не оптимизация, а условие работоспособности.**

---

## 6. Э3 — US-25: аватары, картинки услуг и логотип поверх безопасного загрузчика · P1 *(режется вторым, §3)*

> **Как владелец компании, я хочу поставить фото мастеру и картинку услуге, чтобы витрина выглядела
> живой, а не набором строк.**

**Факт.** `AppUser.AvatarUrl` возвращается в пяти DTO (в том числе в публичном `MasterPublicDto`,
`CompaniesController.cs:100-102`), `Service.ImageUrl` принимается в `CreateServiceDto`
(`DTOs/Services/ServiceDto.cs:12,29`) и отдаётся в `ServiceDto` — но **ни один эндпоинт их не
устанавливает**: загрузки нет, в UI поле не заполняется, значение всегда `null`. Решение Q11 — сделать
загрузку в этом цикле, поверх загрузчика из US-19.

**Критерии приёмки:**
1. `POST /api/profile/avatar` — загрузка аватара текущего пользователя; `POST /api/services/{id}/image` —
   картинка услуги, право по тому же предикату, что правка услуги (только владелец компании и
   `SuperAdmin`, как зафиксировано в цикле A). Оба принимают файл, оба возвращают обновлённый DTO.
2. Обе загрузки используют **тот же код проверки и обработки**, что US-19: сигнатура файла, лимит 5 МБ,
   ресайз, удаление EXIF, rate limiting. Дублирования логики нет — проверяется ревьюером явно.
3. **Класс хранения — публичный:** аватар и картинка услуги показываются анонимному посетителю витрины,
   поэтому лежат в `wwwroot/uploads/...` и раздаются статикой, как логотип. Приватный класс (US-19 п. 4)
   для них не используется. Разница между двумя классами и её причина комментируются в коде.
4. Аватар ресайзится в квадрат меньшего размера (например, 512 px) — он показывается миниатюрой.
5. Старый файл удаляется при замене (как уже сделано для логотипа, `CompaniesController.cs:291-296`).
6. **Существующая загрузка логотипа переводится на новый загрузчик** — это закрывает обе находки ревью
   прошлого цикла (нет проверки содержимого, нет ограничения частоты) одним изменением и не оставляет
   в продукте двух разных путей загрузки картинок.
7. Фронт: загрузка аватара в профиле (`ProfilePage.tsx`), картинки услуги — в форме услуги
   (`owner/CompanyManagePage.tsx`); в публичной витрине и в списке мастеров показывается аватар, если он
   есть, иначе — существующая заглушка с инициалами.
8. Эти изображения **в квоту фото не входят** (квота из US-24 — про приватные фото клиентов);
   ограничение объёма для них — один файл на сущность плюс лимит размера.
9. Тесты: `PROF-0xx` загрузка аватара → `200`, `avatarUrl` не `null`, файл существует; `SVC-0xx`
   картинка услуги мастером → `403`; `CO-0xx` логотип с подменённым `Content-Type` → `400`
   (регрессия на переведённый загрузчик).

---

## 7. Граница цикла: что не входит и почему

### 7.1 Техдолг, который цикл обязан взять

| Что | Почему без него нельзя | Где |
|---|---|---|
| Заметка как объект с `id`, автором и датой | Без этого нет ни удаления заметки, ни фото | US-07 |
| Единый безопасный загрузчик изображений (сигнатура, ресайз, EXIF, rate limiting) | Иначе новая функция повторит пробелы загрузки логотипа | US-19, US-25 п. 6 |
| Точечный rate limiting на загрузку файлов | Первая в продукте точка, где пользователь пишет на диск | US-19 п. 5 |
| Фоновый процесс с гарантией единственного исполнителя | Первый в продукте; нужен уборке по сроку хранения | US-21 п. 1 |
| Фронтовый тест-раннер (Vitest + jsdom) и юнит-тесты новых чистых функций фронта | Цикл добавляет заметный объём UI (загрузка файлов, галерея, форма причины отмены); проверять это только руками — повторение риска R11 цикла A | **US-23** |
| Удаление мёртвого кода (§7.2) | Он мешает читать те же файлы, которые цикл правит | **US-22** |

**Что из прежнего обязательного списка убрано вместе с MAX** — решение аналитика, зафиксировано явно:

- **Часовой пояс компании (`Company.TimeZoneId`).** Заводился ровно ради напоминания «накануне визита»
  и больше ни для чего в этом цикле не нужен: срок хранения фото считается с точностью до суток, и UTC
  тут достаточно. Уезжает в §7.3 вместе с большой темой таймзон. **Возвращается как обязательный в тот
  цикл, где вернутся уведомления** (приложение А, А.5 п. 7) — там без него функция считается неверно.
- **Нормализация телефонов.** Заводилась ради сопоставления «телефон → подписчик MAX». Без уведомлений
  это снова гигиена, а не блокер, и цикл её не берёт. **Но есть следствие, которое надо знать:**
  `MastersController.GetClients` группирует гостей по `GuestPhone` дословно (`:72-74`), поэтому один и
  тот же человек, записанный как «+79990000000» и «+7 999 000 00 00», выглядит двумя клиентами — и
  теперь у него будут **две несвязанные истории с фото**. Это известное ограничение функции, оно
  фиксируется в `CHANGELOG` при выпуске и в риске R7.

### 7.2 US-22 — удаление мёртвого кода и вестигиальных полей · P4

Единой историей, всё перепроверено по коду на `75c5c3c`:

| Что удаляем | Где | Проверка |
|---|---|---|
| `GET /api/bookings/my` и `bookingsApi.getMyBookings` | `BookingsController.cs:273-293`, `frontend/src/api/bookings.ts:32` | Вытеснен `GET /api/bookings/client` — единственный потребитель `ClientBookingsPage.tsx:40`. Ломающее изменение контракта, фиксируется в `API_CONTRACT.md` |
| Кнопка «Отзыв об услуге» в списке мастера | `frontend/src/pages/MyBookingsPage.tsx:196-200` | Условие `canReview` берётся из `GET /api/reviews/can-review`, который отбирает записи по `ClientId == userId`; мастер по своей рабочей записи туда не попадает никогда |
| `CommissionPercent` в `ProfileDto` | `ServiceBooking.API/Controllers/ProfileController.cs:103,107-108` | Комиссия стала per-company в цикле A (`CompanyMember.CommissionPercent`); в профиле значение больше ничего не значит, UI его не показывает |
| `commissionPercent` в `AdminUser` | `frontend/src/api/admin.ts:19` (+ соответствующее поле `AdminUserDto` в `AdminController`) | То же; в `AdminPage.tsx` не используется |
| `pages/DashboardPage.tsx`, `pages/owner/OwnerPage.tsx` | не импортируются ниоткуда; маршруты `/dashboard`, `/owner` — редиректы (`App.tsx:78-79`) | `CURRENT_STATE.md` §5.2 |
| Проект `ServiceBooking/` (Blazor-шаблон) | входит в solution, собирается, не используется | `CURRENT_STATE.md` §5.2 |
| `launchSettings.json` API (`weatherforecast`, порт 5291) | расходится с портом 5000 | `CURRENT_STATE.md` §1 |

**Что, наоборот, оживляется, а не удаляется** (решение Q12): мёртвые ветки в кабинете клиента —
цена визита (`ClientBookingsPage.tsx:139-143`) и «Записаться снова» (`:168-176`). Они не отрисовываются,
потому что `BookingDto` не отдаёт `price` и `companySlug` (`BookingsController.cs:502-506`); оба поля
добавляются в DTO и в `MapToDto` вместе с `cancellationReason` из US-06, одним изменением контракта.

`BookingStatus.Pending` **остаётся** (решение Q10): из enum, TS-типов и фильтров UI не убирается,
миграции не требуется.

**Критерии приёмки US-22:** `dotnet build ServiceBooking.sln` — **0 предупреждений** (после удаления
Blazor-проекта; в CI включается `-warnaserror`); `npx tsc --noEmit` чисто; ни один тест не удалён кроме
тех, что покрывали удалённый эндпоинт (перечисляются в PR и в `TEST_CATALOG.md`).

### 7.3 Что откладывается и почему

| Что | Источник | Почему не сейчас |
|---|---|---|
| **Уведомления в мессенджере MAX целиком** (US-10…US-16) | решение заказчика 2026-09-02 | Тема возвращается к анализу отдельно; исследование и истории сохранены в приложении А |
| **Часовой пояс компании** | §7.1 | Заводился ради напоминаний; без них в цикле не нужен. Возвращается вместе с уведомлениями |
| **Нормализация телефонов** | §7.1 | То же. Следствие — возможное раздвоение истории гостя по формату номера — принято как известное ограничение (R7) |
| Уведомления по **email и SMS**; реальная email-рассылка; честный текст во вкладке «Рассылка» | Q9, `CURRENT_STATE.md` §5.1 п. 1 | Решение Q9: тему не трогаем. Почтового провайдера в проекте нет вовсе |
| **Приём платежей** и настоящая предоплата | `CURRENT_STATE.md` §5.1 п. 2 | Не заказывалось; статусы по-прежнему проставляются вручную |
| **Сценарий «запись ждёт подтверждения салона»** (использование `Pending`) | Q10 | Отдельная история позже; статус в продукте остаётся |
| **Клиент видит свои фото; фото в публичных отзывах** | Q5, §5.1 | Требует отдельного решения по приватности и отдельного экрана; отзывы клиента эта задача не затрагивает |
| **Пагинация длинных списков** (`admin/users` с N+1 по ролям, `admin/companies`, публичные отзывы компании, база клиентов) | старый бэклог US-23-цикла-A, `CURRENT_STATE.md` §9 п. 10 | Контрактное изменение нескольких эндпоинтов; заметно только при росте. **Списки фото сразу проектируются с ограничением выборки**, чтобы не увеличивать долг |
| **Обновление зависимостей фронта** (axios, form-data, react-router — известные уязвимости) | старый бэклог | `react-router` требует мажорного апгрейда и регрессии всего UI; цикл и так добавляет много UI |
| **Security-заголовки и HSTS** | старый бэклог | Инфраструктурная задача devops; в этом цикле фиксируется только требование к ней из US-03 п. 6 |
| **Полноценные таймзоны по всему продукту**, rate limiting на вход/регистрацию, настраиваемый шаг сетки слотов, рефакторинг `CompaniesController`, унификация предикатов прав, деплой с откатом | `CURRENT_STATE.md` §9 | Отдельные циклы; цикл берёт только минимальные куски, без которых Э2/Э3 некорректны |
| **Валидация DTO целиком** (`Slug`, `Bio`, `Comment`, `SendMailDto.Message`), запрет удалять последнего владельца, снятие Identity-роли при `RemoveMember`, проверка статуса в `MarkPaid` | старый бэклог | Некритично; ни один пункт не эксплуатируется |

---

## 8. Нефункциональные требования

1. **Приватность — главный критерий этого цикла.** Фото клиента не доступны никому вне персонала
   компании, где они созданы. Ни один файл с персональными данными не раздаётся статикой; ссылки на
   фото не работают без авторизации. Публичные изображения (логотип, аватар, картинка услуги) — явно
   отдельный класс хранения, и граница между классами описана в коде.
2. **Целостность загрузки.** Ни один файл не попадает на диск, не пройдя проверку сигнатуры, ресайз и
   очистку метаданных. Повторная отправка одной и той же формы (двойной клик, ретрай сети) не создаёт
   дублей фото и не расходует квоту дважды.
3. **Файлы и БД не расходятся.** Порядок операций всегда «сначала БД, потом диск»; сирота на диске
   допустима и подчищается уборкой, потерянный файл при живой строке — нет.
4. **Производительность.** Загрузка и обработка одного фото — не дольше 3 секунд на типовом VPS.
   Экран истории клиента с 50 фото открывается без загрузки полноразмерных изображений. Все новые
   выборки ограничены (`Take`) и не тянут таблицу целиком. Подсчёт занятого объёма — по данным в БД,
   без обхода файловой системы.
5. **Обратная совместимость API.** Формат тел ошибок сохраняется дословно: голая строка для 4xx,
   `ProblemDetails` только для 500 (граница из `ARCHITECTURE.md` §3.1 — она пережила цикл A и остаётся).
   Ломающих изменений контракта в цикле ровно три, все перечисляются в `API_CONTRACT.md`:
   форма заметок (US-07), удаление `GET /api/bookings/my` (US-22), новые поля `BookingDto`
   (US-06 + Q12: `cancellationReason`, `price`, `companySlug`).
6. **Доступность.** Загрузка файлов доступна с клавиатуры; у изображений — осмысленный `alt`; модалка
   просмотра закрывается по Esc; ошибки показываются текстом рядом с элементом, не только цветом.
   Формы причины отмены имеют связанные `label`.
7. **Конвенции проекта не пересматриваются** (`CURRENT_STATE.md` §6): primary constructors, `record`-DTO,
   ручной маппинг, приватные предикаты прав, `pg_advisory_xact_lock` для check-then-act, комментарии
   «почему» на английском; на фронте — именованные экспорты, слой `src/api/*`, react-query, мапперы
   `src/utils/*Error.ts`. Новых библиотек в цикле ровно две: обработка изображений и тест-раннер фронта.
   Каждая — с обоснованием в `ARCHITECTURE.md`.
8. **Тесты.** Функциональный набор не деградирует (сейчас 283 сценария, `TEST_CATALOG.md`); юнит-набор
   `ServiceBooking.UnitTests` работает **без PostgreSQL** и укладывается в 10 секунд; новые чистые
   функции (проверка сигнатуры файла, расчёт квоты и срока хранения, разрешение новых полей тарифа)
   покрываются юнитами. CI остаётся зелёным и получает джоб фронтовых тестов.
9. **Документация как часть работы.** `TEST_CATALOG.md`, `API_CONTRACT.md`, `API_DOCUMENTATION.md`
   обновляются в тех же PR, что и код. `CURRENT_STATE.md` **не редактируется** — он снимок до цикла A;
   расхождения его §4 с кодом фиксируются, но правит его отдельная задача, не эта.

---

## 9. Определение готовности цикла

1. Решения §0 (Q5–Q13, Q16) отражены в коде; ни одна отменённая история (US-01, US-02) не всплыла
   обратно, ни одна история из приложения А не начата.
2. `dotnet build ServiceBooking.sln` — 0 ошибок и **0 предупреждений**; `npx tsc --noEmit` и
   `npm run build` — чисто.
3. `dotnet test ServiceBooking.Tests` — 0 упавших, сценариев не меньше 283 (ожидаемо ~310);
   `dotnet test ServiceBooking.UnitTests` — 0 упавших, < 10 с, при остановленной PostgreSQL.
4. Новый фронтовый набор (`npm run test:run`) — зелёный, в CI отдельным шагом.
5. Миграции применяются на чистой базе и на dev-стенде; `Down` проверен.
6. **Ручной чек-лист QA** (то, чего автотесты не покрывают):
   - мастер прикрепляет 5 фото к заметке, видит их в карточке клиента и в панели под записью; шестое
     фото отклоняется с понятным текстом;
   - фото, снятое телефоном с включённой геолокацией, после загрузки не содержит EXIF (проверяется
     любым просмотрщиком метаданных);
   - прямая ссылка на файл фото, открытая в браузере без авторизации, не отдаёт изображение;
   - сотрудник другой компании не видит ни фото, ни заметок; суперадмин видит объём, но не содержимое;
   - мастер удаляет свою заметку вместе с фото; заметку коллеги удалить не может; владелец компании —
     может; освободившееся место возвращается в квоту;
   - суперадмин выставляет тарифу квоту и срок хранения; владелец видит их в настройках компании;
     при исчерпании квоты загрузка отклоняется с предложением сменить тариф;
   - загрузка аватара и картинки услуги работает; логотип после перевода на новый загрузчик
     по-прежнему загружается, а файл с подменённым типом отклоняется;
   - виджет `/embed/<slug>`, вставленный в тестовый HTML, работает; на нём проходит запись;
   - суперадмин блокирует и разблокирует компанию; заблокированная компания недоступна публично;
   - деактивированный тариф активируется обратно и назначается владельцу;
   - отмена записи с причиной: причина видна второй стороне; в кабинете клиента видны цена визита и
     кнопка «Записаться снова»;
   - «Мои визиты» доступны из меню владельцу и мастеру.
7. `CHANGELOG.md` и раздел «О проекте» в `README.md` обновлены по фактически поставленному (вызов 2
   аналитика), гайды — technical-writer. Если US-21 была срезана — это в `CHANGELOG` названо прямо.

---

## 10. Риски

| # | Риск | Вероятность / эффект | Что делаем |
|---|---|---|---|
| **R1** | Фото занимают диск VPS и валят сервис | Средняя / **высокий** | Ресайз всегда, квота из тарифа, срок хранения, порог свободного места (US-19, US-21, US-24). Оценка ёмкости — §5.9 п. 6 |
| **R2** | Утечка фото клиентов через угаданную ссылку или ошибку в правах | Низкая / **очень высокий** (персональные данные) | Файлы вне `wwwroot`, раздача только через авторизованный эндпоинт, тест на анонимный доступ обязателен (US-19 п. 8), ручная проверка в DoD |
| **R3** | Новые поля тарифа ломают разрешение подписки — а `SubscriptionResolver` только что стабилизировали в цикле A | Средняя / высокий | Поля проходят тот же путь, что остальные возможности, с явным Free-базлайном (US-24 п. 2); юнит-кейсы на Free и на неактивный план обязательны |
| **R4** | Первый в продукте фоновый процесс (US-21) сделан наспех: два исполнителя, удаление лишнего, зависшая транзакция | Средняя / **высокий** (удаляем данные) | Advisory lock, работа порциями, сначала БД — потом диск, журнал удалений. При нехватке времени история режется целиком и заменяется ручной процедурой, а не делается наполовину |
| **R5** | Ломающее изменение формы заметок (US-07) роняет два экрана, которые их показывают | Средняя / средний | BE и FE едут одним PR; оба экрана перечислены в истории; ручная проверка в чек-листе |
| **R6** | Правовой риск по фото: согласия клиента продукт не спрашивает (решение Q6) | Средняя / **высокий** | Риск **принят заказчиком осознанно**; команда его не закрывает и не изобретает механику согласия. Зафиксировано здесь и в §0, чтобы при выходе в продакшен вопрос был поднят заново |
| **R7** | История гостя раздваивается из-за разных форматов телефона, и фото попадают в «разных» клиентов | Средняя / средний | Нормализация телефонов вынесена из цикла осознанно (§7.1). Ограничение фиксируется в `CHANGELOG`; сотрудник может объединить историю, записав гостя тем же номером |
| **R8** | Библиотека обработки изображений тянет лицензионные или платформенные проблемы (ImageSharp сменил лицензию; SkiaSharp требует нативных зависимостей в контейнере) | Средняя / средний | Выбор делает архитектор до кодирования (§11 п. 1), с проверкой сборки в докер-образе, а не только на машине разработчика |
| **R9** | Скоуп не влезает | Средняя / средний (без MAX цикл заметно легче) | Порядок урезания зафиксирован заранее (§3); US-07 и US-19 не режутся. Новые находки в ходе работы **документируются в SPEC следующего цикла**, а не чинятся на ходу — правило, которое сработало в цикле A |

---

## 11. Открытые вопросы архитектору

Не решения, а точки, которые нужно закрыть до кодирования.

1. **Библиотека обработки изображений** под `net8.0`, её лицензия и нативные зависимости в докер-образе
   (`ServiceBooking.API/Dockerfile`, `aspnet:8.0`). Решение фиксируется в `ARCHITECTURE.md` с
   обоснованием; проверка — сборка образа, а не только локальный `dotnet run`.
2. **Два класса хранения файлов** (публичный `wwwroot` и приватный каталог вне его): где проходит
   граница в коде, как один загрузчик обслуживает оба, и как это ложится на два деплой-контура —
   docker volume на Linux VPS (`docker-compose.prod.yml`) и IIS на Windows (`DEPLOY-windows.md`).
   Путь — из конфигурации, не захардкожен.
3. **Форма связи «заметка ↔ фото ↔ визит»**: отдельная таблица фото со ссылкой на заметку (эскиз §5.2)
   и `ClientNote.BookingId` — подтвердить или предложить лучше, с учётом того, что заметка может
   относиться и к гостю без аккаунта (ключ — телефон).
4. **Учёт занятого объёма.** Счётчик на компанию в БД или агрегат по таблице фото; как он остаётся
   верным при параллельных загрузках и удалениях (конвенция проекта — транзакция + advisory lock).
5. **Как новые поля тарифа проходят через `SubscriptionResolver` и `EffectivePlan`**, не ломая
   батчевое разрешение без N+1 и Free-базлайн.
6. **Форма фонового процесса** (US-21): единственность исполнителя при более чем одном экземпляре
   приложения, поведение при деплое через `docker compose up -d --build`, размер порции.
7. **Rate limiting**: встроенный `Microsoft.AspNetCore.RateLimiting` или ручной счётчик; как он
   уживается с тем, что аутентификация уже делает запрос к БД на каждый вызов (`Program.cs`,
   `OnTokenValidated`).
8. **Порядок миграций** цикла и их взаимная независимость: `ClientNote.BookingId`, таблица фото,
   поля тарифа (квота и срок хранения).
9. **Фронтовый тест-раннер** (US-23): состав (Vitest + jsdom + Testing Library), что покрываем в первую
   очередь и как это встраивается в существующий джоб фронтенда в CI без роста суммарного времени
   прогона выше 10 минут.

---

# Приложение А. Уведомления через мессенджер MAX — исследование, отложено

**Решение заказчика (2026-09-02): в этот цикл интеграция с MAX не входит.** Тема возвращается к
анализу отдельно. Материал ниже — результат проведённого исследования; он сохранён целиком, чтобы
не проводить его заново, и **не является частью скоупа**. Истории US-10…US-16 здесь приведены в том
виде, в каком они были написаны; их номера не переиспользуются в основном тексте.

### А.0 Вопросы, которые останутся открытыми при возврате к теме

Все они упираются в выбор способа отправки и без него не решаются. Перенесены сюда из §0.

| # | Вопрос |
|---|---|
| **Q1** | **Какой из вариантов отправки берём** — A (бот платформы), B (бот компании), C (бизнес-аккаунт через провайдера) или четвёртый, неофициальный (шлюзы к личному аккаунту, см. ниже)? От ответа зависит, есть ли экран «привязать MAX», кто платит и достижим ли сценарий «клиент ничего не делает» |
| **Q2** | **Кто юридически регистрирует бота?** С августа 2025 вывод ботов MAX в продакшн доступен только верифицированным организациям — юрлицам и ИП, резидентам РФ. Для варианта A нужен ИНН у платформы, для варианта B — у каждого салона. Если ИНН нет ни у кого — функция недостижима в принципе |
| **Q3** | **Приемлемо ли, что уведомление получит только клиент, который сам подписался на бота?** В вариантах A и B иначе никак; реалистичный охват — 20–50 % |
| **Q4** | **Часовые пояса.** Напоминание «накануне визита» без `Company.TimeZoneId` считается неверно (`CURRENT_STATE.md` §9 P1-6). Рекомендация: одно поле, по умолчанию `Europe/Moscow`, без общей ревизии таймзон |
| **Q14** | **Уведомления и тариф.** Существующее `NotifyDaysBefore` — про уведомление до деактивации подписки (`PlansTab.tsx:188-190`), переиспользовать его нельзя. Нужен отдельный флаг возможности и настройка «за сколько часов напоминать» на уровне компании; на каких тарифах функция доступна — решение заказчика |
| **Q15** | **Смена владельца компании при привязанном канале.** Рекомендация: канал деактивируется (fail-closed), новый владелец подтверждает привязку заново — токен прежнего владельца молча передавать нельзя |

**Что добавилось к исследованию после его написания.** Помимо трёх описанных ниже вариантов
(бот платформы, бот компании, официальный бизнес-аккаунт через провайдера) существует четвёртый,
не отражённый в А.1: сторонние шлюзы, авторизующие **личный аккаунт** владельца по номеру телефона
(Wappi, GREEN-API, WAMM, TWIN). Технически он единственный отвечает исходной формулировке — сообщения
уходят от имени и с номера владельца, получателю не нужно ни на что подписываться, писать первым
можно. Стоимость невелика (у GREEN-API бесплатный тариф на 3 чата, далее ~690 ₽/мес).

Но это неофициальный доступ: платформа его не документирует и не одобряет, а документация самих
шлюзов прямо предупреждает о риске блокировки. Причины блокировок в MAX — автоматизация отправки,
однотипные сообщения многим получателям, необычно высокая скорость, входы с разных устройств — то
есть ровно то, чем является рассылка напоминаний. Существенно: **риск ложится на личный аккаунт
владельца салона**, и блокировка означает потерю личного номера в мессенджере, а не отключение
функции в сервисе. Если вариант будет рассматриваться — предлагать его владельцу нужно явно, с
честным описанием риска, и не делать поведением по умолчанию.

Источники по четвёртому варианту: [wappi.pro](https://wappi.pro/max-api-documentation),
[green-api.com/max](https://green-api.com/max), [wamm.chat](https://wamm.chat/help/opisanie-api),
[причины блокировки](https://green-api.com/v3/docs/faq/reasons-for-blocking-MAX-account/).

### А.1 Исходное исследование выполнимости (варианты A, B, C)

Формулировка задачи — «уведомления уходят **от имени и с номера владельца**» — **технически
недостижима** официальными средствами, и это не вопрос трудозатрат. Ниже — что удалось установить.

**Что даёт MAX Bot API** (официальная документация для разработчиков, `dev.max.ru`):

- Это REST-интерфейс: методы для ботов, чатов, сообщений, комментариев, подписок (webhook / long
  polling) и загрузки файлов. Токен выдаёт Master Bot внутри мессенджера при создании бота; авторизация —
  заголовком `Authorization`.
- **Бот не может написать первым человеку, который не начинал с ним диалог.** Отправка возможна в чат,
  идентифицированный `chat_id`, то есть после того, как пользователь открыл бота и нажал «Начать».
- **Отправки по номеру телефона в Bot API нет.** Практикующие разработчики решают это так: бот просит
  пользователя «поделиться контактом», сервис сохраняет пару «телефон → chat_id» и дальше шлёт по
  телефону через собственный слой сопоставления. То есть подписка клиента всё равно обязательна.
- Ограничение частоты — **30 запросов в секунду** к `platform-api2.max.ru`, при превышении `429`.
  Webhook принимается только по HTTPS с доверенным сертификатом.
- Создание бота и использование Bot API бесплатны, комиссии платформа не берёт.
- **С августа 2025 вывод ботов в продакшн доступен только верифицированным организациям — юрлицам и
  ИП, резидентам РФ.** Физлица и самозанятые на платформу разработчика не допускаются. Мини-приложения
  проходят модерацию.

**Что даёт официальный бизнес-аккаунт MAX** (подключается не напрямую, а через провайдеров — edna,
i-Digital, Messaggio, evatelecom и др.):

- Верифицированный бизнес-аккаунт (галочка) с подтверждением по документам; заявленный срок верификации
  у провайдеров — от суток до 3–10 рабочих дней.
- **Рассылка сервисных уведомлений и OTP по номеру телефона** — подтверждения записи, напоминания,
  статусы, ссылки на оплату. Предварительная подписка клиента на бота не требуется.
- Требуется российский (или белорусский) номер, регистрационные документы компании, ИНН; чат-боты
  верифицированного аккаунта проходят модерацию. Разделяются сервисные и рекламные сообщения.
- Платно: лицензия платформы-провайдера от порядка 6 000 ₽/мес плюс тарификация сообщений; конкретика
  зависит от провайдера и обсуждается договором.

**Чего нет ни в одном официальном варианте.** Отправки сообщений **от имени личного аккаунта владельца
и с его номера**. Публичного user-API у MAX нет; автоматизация личного аккаунта не входит в
поддерживаемые сценарии и противоречит правилам платформы (прямой запрет массовой неавторизованной
автоматизации). Неофициальный путь существует (четвёртый вариант выше), но переносит риск блокировки
на личный аккаунт владельца.

**Три официально достижимых варианта:**

| | **A. Один бот платформы** | **B. Бот компании (рекомендуется)** | **C. Бизнес-аккаунт через провайдера** |
|---|---|---|---|
| Кого клиент видит отправителем | «EZBOOK» | «Салон Ромашка» (имя и аватар бота задаёт владелец) | Верифицированный бизнес-аккаунт салона или платформы |
| Нужна ли клиенту подписка | Да, один раз | Да, один раз | **Нет**, шлётся на номер телефона |
| Что «привязывает» владелец | Ничего (нечего) | Токен своего бота из MAX + подтверждение владения | Договор с провайдером + документы |
| Требования к владельцу | Нет | ИП/юрлицо РФ, верификация на dev.max.ru | ИНН, документы, модерация шаблонов |
| Деньги | 0 | 0 | Лицензия от ~6 000 ₽/мес + за сообщения |
| Реалистичный охват клиентов | Низкий | Низкий-средний | Высокий |
| Срок до первого сообщения | Дни | Дни (плюс верификация владельца) | Недели |

**Рекомендация аналитика:** брать **B** как основной сценарий — он единственный отвечает исходной
формулировке «владелец привязывает свой аккаунт», и в чате клиент видит салон, а не платформу. Схему
данных при этом строить так, чтобы **A** был частным случаем (компания без своего токена использует
общий платформенный), а **C** — будущим третьим типом канала, не ломающим модель. **C** самостоятельно
не проектируем: это коммерческое решение с договором.

**Честная формулировка для клиента**, которую нужно поставить в интерфейс и в маркетинг:
«Клиент получит напоминание в MAX, если он один раз откроет бота вашего салона и нажмёт "Начать".
Сообщение придёт от бота салона, а не с вашего личного номера — присылать сообщения от имени личного
аккаунта мессенджер не позволяет никому».

### А.2 Чего в проекте нет и что придётся построить

Это **инфраструктурная работа**, а не только интеграция:

- **Нет ни одного фонового процесса.** Ни `BackgroundService`, ни `IHostedService`, ни Hangfire/Quartz
  (`CURRENT_STATE.md` §5.1 п. 3 — подтверждено). Напоминание «за N часов до визита» некому запустить.
  *(Замечание при возврате к теме: цикл B вводит первый фоновый процесс — уборку фото по сроку
  хранения, US-21. Если он уже поставлен, инфраструктурная часть будет дешевле.)*
- **Нет очереди и нет исходящего журнала.** `MailLog` — журнал «рассылок», к доставке отношения не имеет.
- **`NotifyDaysBefore` в тарифе не про это** (Q14): в админке оно подписано «Уведомление за N дн. до
  деактивации» (`PlansTab.tsx:188-190`) и ни на что не влияет.
- **Нет часовых поясов** (Q4) — а «накануне визита» без них не считается.
- **Нет нормализации телефонов**: «+79990000000» и «+7 999 000 00 00» — разные строки
  (`CURRENT_STATE.md` §9, раздел D старого бэклога). Для сопоставления «телефон → подписчик MAX» это
  становится блокирующим, а не косметическим.

### А.3 US-10 — привязка канала MAX к компании

> **Как владелец компании, я хочу подключить свой канал в MAX, чтобы мои клиенты получали уведомления
> от имени моего салона.**

**Критерии приёмки** (сформулированы под рекомендованный вариант B; при выборе A история сводится к
включению тумблера, при выборе C — переписывается):

1. Новая сущность канала уведомлений уровня компании: компания, тип канала (`PlatformBot` / `CompanyBot`),
   зашифрованный токен, идентификатор и имя бота (подтягиваются из MAX), статус
   (`Draft` / `Verifying` / `Active` / `Disabled` / `Error`), время последней успешной проверки,
   кто и когда привязал. **Одна компания — не более одного активного канала.**
2. Экран «Уведомления» во вкладке «Настройки» управления компанией: пошаговая инструкция (создать бота
   через Master Bot в MAX → скопировать токен → вставить сюда), поле токена, кнопка «Проверить и
   подключить».
3. При подключении сервер **проверяет токен живым вызовом** к MAX (получение информации о боте). Успех →
   показываются имя и @-адрес бота, статус `Active`. Неверный токен → `400` с текстом «Токен не принят
   мессенджером MAX» (без сырого ответа внешнего сервиса в теле).
4. **Токен никогда не возвращается наружу.** `GET` отдаёт только маску (последние 4 символа), имя бота и
   статус. Токен не попадает в логи, в сообщения об ошибках, в Swagger-примеры и в ответы админки.
5. Токен хранится **зашифрованным** (ASP.NET Core Data Protection с ключами из конфигурации/переменных
   окружения, по конвенции секретов проекта — `.env.production`). Ключ шифрования — обязательный секрет,
   без которого приложение не стартует (по образцу fail-fast из цикла A).
6. Привязку и отвязку выполняет **только владелец компании** (`CanManageCompany`) или `SuperAdmin`;
   мастер не видит экрана вовсе.
7. Гейт тарифа: экран доступен, только если тариф владельца допускает уведомления (Q14). Иначе —
   заглушка «доступно на тарифе …», без 402 после заполнения формы (конвенция трёхуровневых флагов
   `CompanyDto`, `CURRENT_STATE.md` §4.6).
8. Тесты: `NTF-0xx` посторонний привязывает канал → `403`; `NTF-0xy` неверный токен → `400`, строка не
   создана; `NTF-0xz` `GET` не содержит токена ни в каком виде (проверяется поиском подстроки в теле
   ответа); `NTF-0xw` вторая привязка к той же компании заменяет первую, а не создаёт вторую.

### А.4 US-11 — отвязка, смена владельца, деактивация

> **Как владелец, я хочу отключить канал в один клик и быть уверенным, что мой токен удалён.**

**Критерии приёмки:**
1. Кнопка «Отключить» с подтверждением. После неё: токен **физически удаляется** (не «помечается»),
   статус `Disabled`, все ещё не отправленные уведомления этой компании в очереди переводятся в
   `Cancelled` с причиной «канал отключён».
2. Подписки клиентов (сопоставление «телефон → chat_id»), полученные через бота компании, при отвязке
   **удаляются** — они принадлежат чужому боту и без него бессмысленны и небезопасны для хранения.
   *Предположение: удалять, а не сохранять «на случай возврата» (низкий риск, но подтвердить в Q1).*
3. **Смена владельца компании** (`PUT /api/admin/companies/{id}/owner`) переводит канал в `Disabled`
   и удаляет токен; новый владелец видит подсказку «канал отключён при смене владельца, подключите
   заново» (Q15).
4. Блокировка компании (US-04) переводит канал в `Disabled`, но токен сохраняет; разблокировка требует
   явного включения владельцем. *Предположение (низкий риск).*
5. Отзыв тарифа/переход на тариф без уведомлений: канал остаётся привязанным, но отправка не
   выполняется; в очередь ничего не ставится, а в интерфейсе показано, почему.
6. Тесты: `NTF-0xx` после отвязки токен отсутствует в БД (проверка прямым запросом); `NTF-0xy` смена
   владельца отключает канал; `NTF-0xz` уведомления, стоявшие в очереди, не уходят после отвязки.

### А.5 US-12 — фоновая отправка: очередь, планировщик, ретраи

> **Как владелец, я хочу, чтобы уведомления уходили сами и вовремя, а сбой мессенджера не ломал запись
> клиента.**

**Критерии приёмки:**
1. Новая таблица исходящих уведомлений: компания, получатель (клиент или гость по телефону), ссылка на
   запись, тип уведомления, момент отправки (`ScheduledAtUtc`), статус
   (`Pending`/`Sent`/`Failed`/`Skipped`/`Cancelled`), число попыток, время последней попытки, текст
   ошибки, **ключ идемпотентности** (тип + запись + получатель), уникальный индекс по нему.
2. **Создание записи никогда не ждёт мессенджер.** `POST /api/bookings` только кладёт строку в очередь в
   той же транзакции; исходящий HTTP-вызов делает фоновый обработчик. Ошибка постановки в очередь **не**
   отменяет запись (логируется, запись создаётся).
3. Фоновый обработчик — один `BackgroundService`, опрос очереди с интервалом (по умолчанию 60 секунд),
   выборка «созревших» строк с блокировкой, чтобы два экземпляра приложения не отправили дважды
   (конвенция проекта — `pg_advisory_xact_lock`, `Services/AdvisoryLock.cs`).
4. **Ретраи:** до 5 попыток с экспоненциальной паузой (1, 5, 15, 60, 180 минут). `429` от MAX —
   не считается неуспешной попыткой по существу: пауза берётся с уважением к лимиту 30 rps и повтор
   планируется. После исчерпания попыток — `Failed` с сохранённой причиной.
5. **Просроченные не отправляются:** если момент визита уже прошёл, напоминание переводится в `Skipped`
   с причиной «устарело», а не отправляется с опозданием.
6. Отмена/перенос записи **отменяет** невыполненные напоминания по ней (`Cancelled`) и ставит новые под
   новую дату.
7. **Время считается в часовом поясе компании** (`Company.TimeZoneId`, Q4). Напоминание «за N часов» и
   «накануне в HH:MM» вычисляются от локального времени визита. Значение по умолчанию — `Europe/Moscow`,
   поле редактируется владельцем в настройках компании.
8. Отправка исходящих ограничена по частоте так, чтобы не превышать 30 rps суммарно к API MAX.
9. Юнит-тесты (проект `ServiceBooking.UnitTests`, без БД): планирование момента отправки для разных
   TZ и разных `N`; переход по статусам и расчёт паузы ретрая; идемпотентность (повторная постановка
   не создаёт вторую строку). Не менее 12 кейсов.
10. Функциональные тесты: `NTF-1xx` создание записи ставит ровно два уведомления (подтверждение +
    напоминание); `NTF-1xy` отмена переводит невыполненные в `Cancelled`; `NTF-1xz` перенос
    перепланирует напоминание.
11. Приложение поднимается и работает **без сконфигурированного MAX**: обработчик видит, что каналов нет,
    и ничего не делает. Тестовый и dev-контур не должны ходить в интернет
    (по образцу `CaptchaService.IsEnforced`).

### А.6 US-13 — подписка клиента на канал

> **Как клиент, я хочу один раз подключить MAX, чтобы получать напоминания о визитах.**

**Критерии приёмки:**
1. После успешной записи (в `BookingModal` и в кабинете клиента) показывается блок «Получать
   напоминания в MAX» со ссылкой-переходом в бота и коротким объяснением: одно нажатие «Начать» и
   «Поделиться контактом».
2. Сервер принимает вебхук от MAX (HTTPS, отдельный эндпоинт, проверка секрета/подписи запроса) и
   сохраняет подписку: компания (канал), нормализованный телефон, `chat_id`, дата подписки.
3. **Телефон нормализуется** к единому виду (только цифры, с ведущим кодом страны) и при подписке, и при
   поиске получателя. Нормализация применяется в одном месте и покрыта юнит-тестами (не менее 8 кейсов:
   пробелы, скобки, `8` вместо `+7`, дефисы, мусор).
4. Если получателя не нашли — уведомление переводится в `Skipped` с причиной «клиент не подписан»;
   ошибки это не считается, владельцу это показывается как «не доставлено: клиент не подключил MAX».
5. Клиент может отписаться (командой боту); подписка помечается неактивной, дальше — `Skipped`.
6. Вебхук-эндпоинт **не требует авторизации пользователя, но требует секрет** и устойчив к повторной
   доставке одного события (идемпотентность по идентификатору события).
7. Тесты: `NTF-2xx` вебхук создаёт подписку; `NTF-2xy` повторная доставка не создаёт дубль;
   `NTF-2xz` вебхук без секрета → `401`; `NTF-2xw` уведомление клиенту без подписки → `Skipped`.

### А.7 US-14 — какие уведомления и когда

> **Как клиент, я хочу получить подтверждение сразу и напоминание перед визитом.**

**Критерии приёмки:**
1. Реализуются **ровно два типа** уведомлений клиенту:
   - **Подтверждение записи** — сразу после создания записи (любым путём: клиент, гость, ручная запись
     персонала);
   - **Напоминание** — за настраиваемое время до визита (настройка компании, по умолчанию 24 часа;
     допустимый диапазон 1–72 часа).
2. Тексты — шаблонами с подстановками: название компании, услуга, мастер, дата и время (в TZ компании),
   адрес, телефон компании. Шаблоны хранятся в коде, не редактируются владельцем.
3. В сообщении **нет персональных данных третьих лиц** и нет ссылок на внутренние идентификаторы.
4. Отмена/перенос как отдельные уведомления — за границей той итерации, но модель типов должна их
   допускать без миграции схемы.
5. Владелец может выключить каждый тип отдельно в настройках компании.
6. Тексты согласуются с technical-writer при выпуске; их черновик — часть этой истории.
7. Тесты: `NTF-3xx` подтверждение ставится в очередь на «сейчас»; `NTF-3xy` напоминание — на
   «время визита минус N часов» в TZ компании; `NTF-3xz` при выключенном типе уведомление не ставится.

### А.8 US-15 — владелец видит, что доставлено

> **Как владелец, я хочу понимать, дошло ли напоминание, чтобы не гадать, почему клиент не пришёл.**

**Критерии приёмки:**
1. В управлении компанией — «Журнал уведомлений»: последние 100 записей с датой, типом, получателем
   (имя + маскированный телефон), статусом и человеческой причиной неуспеха.
2. Причины формулируются по-русски и без технических кодов: «клиент не подключил MAX», «мессенджер
   временно недоступен, повторим», «канал отключён».
3. В карточке записи в кабинете мастера — компактная отметка о статусе напоминания.
4. Список ограничен `Take(100)` и не тянет всю таблицу.
5. Доступ — только персонал компании.

### А.9 US-16 — уведомления персоналу

> **Как мастер, я хочу получать в MAX сообщение о новой записи ко мне.**

**Критерии приёмки:** мастер и владелец могут подписаться на канал компании так же, как клиент;
уведомления «новая запись», «запись отменена клиентом» уходят по тому же механизму; настройка
включается на уровне пользователя в компании. Отдельных сущностей это не добавляет.
